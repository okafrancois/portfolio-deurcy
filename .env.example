# Admin
ADMIN_PASSWORD=change-me
ADMIN_SESSION_SECRET=replace-with-32+-random-chars
ADMIN_TOKEN=replace-with-32+-random-chars  # must match the value set on Convex (`npx convex env set ADMIN_TOKEN ...`)

# Email (Resend) — optional but recommended for quote notifications
RESEND_API_KEY=
RESEND_FROM=Portfolio <onboarding@resend.dev>
OWNER_EMAIL=D3visionprod@gmail.com

# Convex (auto-filled by `npx convex dev` on first run)
# NEXT_PUBLIC_CONVEX_URL=
# CONVEX_DEPLOYMENT=
